SUPABASE_URL=https://mxgdkgunszcfxqhsutqy.supabase.co
SUPABASE_ANON_KEY=sb_publishable_tkxO7IjjBaujruU3Wjdjzw_pb_7ePG5
  already loaded. It merges instead of failing.
  
  Now run a clean build to clear any stale asset cache:
  
  flutter clean && flutter pub get && flutter run
  
  This is the real fix — flutter clean forces Flutter to re-bundle all assets including .env, which resolves the FileNotFoundError that
  appears on restart when the build cache gets out of